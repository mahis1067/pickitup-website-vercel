# HTML templates

This folder contains the website's presentational TSX. In Next.js, TSX is the HTML template layer: it produces the page markup while still supporting reusable components and typed props.

Keep Sanity queries, mutations, and other backend concerns out of this folder. Templates should receive content through props so they can be developed with fixture data before the backend is connected.